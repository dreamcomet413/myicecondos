<nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
              data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand page-scroll" href="/">
        <%= image_tag "logo.png", width: "75", height: "29", class: "pull-left", style: "margin-top: -6px;" %>
        <span class="xsmall-font">TORONTO’S MOST LUXURIOUS CONDOMINIUMS</span></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% l = MenuLocation.where(name: "Header").first %>
        <% l.menu_items.each do |m| %>
          <li class="visible-xs"><%= link_to m.title, m.url %></li>
        <% end if l %>
        <% if user_signed_in? %>
          <li><%= link_to "Favourites", favourites_path %></li>
          <li><%= link_to "My Account", my_account_path %></li>
          <li><%= link_to('Log Out', destroy_user_session_path, :method => :delete) %></li>
        <% else %>
          <li><a href="#modal-login" data-toggle="modal">LOGIN / REGISTER</a></li>
        <% end %>
        <li><a href="#">CONTACT US</a></li>
        <li>
          <a href="#" class="menu-toggle hidden-xs">
            <div class="hamburger">
              <span></span>
            </div>
            &nbsp;&nbsp;MENU
          </a>
        </li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
<div class="nav-menu">
  <div id="menu-scroll" class="nano">
    <div class="nano-content">
      <ul class="nav">
        <% l = MenuLocation.where(name: "Header").first %>
        <% l.menu_items.each do |m| %>
          <li class="text-uppercase"><%= link_to m.title, m.url %></li>
        <% end if l %>
      </ul>
    </div>
  </div>
</div>
